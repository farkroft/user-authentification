image: golang:1.12

before_script:
    - mkdir -p /go/src/gitlab.com/auth-service
    # - export GOPATH=/go

stages:
    - test

test:
    stage: test
    script:
        - cd /go/src/gitlab.com/auth-service
        - make test
    